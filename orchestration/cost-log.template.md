# 実績ログ(WP単位の役割分担とトークン消費)

> 目的: 見積り精度の向上(model-routing.mdのパターン帯とagent_usage.pyの係数を実績で較正する)。
> 記録ルール: WP完了ごとに1行+セッション末尾に「指令塔(実測)」行と「従量合計」行。
> 委譲分は `agent_usage.py <サブエージェントのJSONL>`、指令塔分は `agent_usage.py --main <メインセッションのJSONL>` の実測値(v0.4.2以降、指令塔も推定ではなく実測)。
> 列: 差し戻し=検収での差し戻し回数。結果=合格/条件付き/やり直し。USDは従量モデル分のみ実額、枠内モデルは「枠内 ¥0」。

| 日付 | 項目(WP or 指令塔) | パターン | 非ｷｬｯｼｭ入力 | ｷｬｯｼｭ書込 | ｷｬｯｼｭ読取 | 出力 | USD | 見積り比 | 差し戻し | 結果 | 備考 |
|---|---|---|---|---|---|---|---|---|---|---|---|
